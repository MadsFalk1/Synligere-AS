---
import BaseLayout from '../layouts/BaseLayout.astro';

const cards = [
	{
		emoji: '🍽️',
		title: 'Vi skal ut og spise',
		text: '',
	},
	{
		emoji: '🎬',
		title: 'Vi ser en Disney-film sammen',
		text: '',
	},
	{
		emoji: '🍰',
		title: 'Vi lager brownie med is sammen',
		text: '',
	},
	{
		emoji: '💬',
		title: 'Fra meg til deg',
		text: 'Jeg er kanskje ikke på mitt beste i det siste, men jeg elsker deg, og jeg vil bruke dagen med deg.',
	},
];
---

<BaseLayout title="Plan | Synligere" description="Planoversikt.">
	<section class="mx-auto w-full max-w-lg px-4 py-10">
		<p class="text-xs font-semibold uppercase tracking-[0.2em] text-[#b56449]">Plan</p>
		<h1 class="mt-2 text-3xl font-bold tracking-tight text-[#1a1a1a]">Plan for oss 💛</h1>
		<div id="plan-cards" class="mt-6 grid gap-4">
			{cards.map((card, index) => (
				<article data-card-index={index} class="plan-card rounded-3xl border border-[#24140f]/10 bg-white p-5 opacity-0 translate-y-4 shadow-[0_14px_32px_-26px_rgba(52,34,24,0.45)] transition duration-500 ease-out">
					<p class="text-2xl">{card.emoji}</p>
					<h2 class="mt-2 text-lg font-semibold text-[#1f1a17]">{card.title}</h2>
					{card.text && <p class="mt-1 text-sm leading-relaxed text-[#4d433e]">{card.text}</p>}
				</article>
			))}
		</div>
	</section>
</BaseLayout>

<script>
	const cards = document.querySelectorAll('.plan-card');
	cards.forEach((card, index) => {
		setTimeout(() => {
			card.classList.remove('opacity-0', 'translate-y-4');
		}, 220 * index);
	});
</script>
